# Controller

Future home of the externally visible AllfatherChess UCI engine and meta-controller.

The controller will own search-region allocation, resource scheduling, residual estimation, verification/re-lock, fallback, and final move selection.

The first milestone intentionally contains no behavioral routing code.
